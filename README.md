# TypeScript Assignment

## File Structure

```
├── solutions.ts   — All 7 coding solutions
├── blog-1.md      — Blog: why unknown is safer than any + type narrowing
├── blog-2.md      — Blog: Pick and Omit utility types
└── README.md      — This file
```

## Problems Solved

| Problem | Description |
|---|---|
| 1 | `filterEvenNumbers` — filters even numbers from an array |
| 2 | `reverseString` — reverses a string |
| 3 | `checkType` — uses union type and type guards |
| 4 | `getProperty` — generic function with key constraint |
| 5 | `toggleReadStatus` — extends a Book interface |
| 6 | `Person` and `Student` classes with inheritance |
| 7 | `getIntersection` — finds common elements in two arrays |

## Blog Topics Covered

- **Blog 1:** Why `any` is unsafe and `unknown` is the better choice, with type narrowing explained
- **Blog 2:** How `Pick` and `Omit` utility types keep code DRY
